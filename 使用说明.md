# step.conf：把泛函 FUNC 从脚本挪到项目模板

本次只把 **FUNC** 接上，但底层机制（每步一个文件夹、分层合并、`tf conf` 查改）
是通用的，之后加参数只要往 `step.conf` 里写键、在脚本的 `CONF_SPEC` 里声明即可。

---

## 一、为什么必须这么做

`tf` 的 `remote_gen()` 里对 gen 脚本是「以 skill 为唯一样板，总是覆盖推送」，
所以 `gen_step1_PBE_opt.py` 顶部的 `FUNC = "pbesol"` 你在项目里怎么改都会被冲掉。
模板文件走的是 `find_asset()` 查找链，是项目私有的——**参数必须住在模板侧**。

## 二、文件清单

| 文件 | 放哪 | 说明 |
|---|---|---|
| `skill_band/stepconf.py` | `skill/band/` | 新增。解析/合并/改写 `step.conf`，纯标准库 |
| `skill_band/templates/step.conf` | `skill/band/templates/` | 新增。band 技能的**出厂默认**（`FUNC = pbesol`） |
| `skill_band/gen_step1_PBE_opt.py` | `skill/band/` | 已改。FUNC 从 `step.conf` 读 |
| `skill_band/skill.yaml` | `skill/band/` | 已改。`gen_need` 加 `stepconf.py` + `step.conf` |
| `tf.patch` | 仓库根 | tf 主程序 4 处补丁 |
| `skill_band.patch` | 仓库根 | 上面两个 skill 文件的 diff（想手动看改了啥时用） |

`stepconf.py` 与 `dim_common.py` 一样，**每个技能各放一份**（内容相同，
tf 按 md5 自动同步推送）。现在只有 band 用到，elastic / ke 等以后接的时候再拷。

## 三、安装

```bash
cd ~/taskflow
git apply tf.patch                       # tf 主程序 4 处
cp stepconf.py            skill/band/
cp gen_step1_PBE_opt.py   skill/band/
cp skill.yaml             skill/band/
cp templates/step.conf    skill/band/templates/

python3 -c "import ast;ast.parse(open('versions/v1.0/tf',encoding='utf-8').read())"   # 语法自检
```

tf.patch 的 4 处改动：

1. **`find_asset()`** —— 项目侧新增「按步骤分目录」这一层。
   查找优先级变成 `<根>/templates/<步骤名>/` > `<根>/templates/` > `<根>/`，
   根依次是 `材料/<技能>/`、`project_setting/`，最后才是 skill 库。
2. **`remote_gen()`** —— `step.conf` 不按普通文件推送，先在本地把各层合并成
   一份带 `# <- [N]` 来源注释的文本再推。超算上永远只有一份、零回落逻辑。
3. **`cmd_init()`** —— 递归下发整棵 `templates/` 树（保留 `<步骤名>/` 子目录，
   含 `*.tpl` 与 `*.conf`）。原代码对 per_step 布局是直接 `continue` 掉的。
4. **新增 `tf ... conf` 命令** —— 查看某步的最终生效值与来源；`--set` 修改。

## 四、初始化一个材料

```bash
tf -p Mg2C60 init
```

会在 `Fullerene_Network/Mg2C60/band/project_setting/templates/` 下铺出：

```
templates/
├── step.conf            ← 本材料所有步骤共用（当前只有 FUNC）
├── incar_2d.tpl  incar_3d.tpl
└── submit_jzzn_vasp*_*.tpl
```

要给某个步骤单独设值，自己建同名文件夹即可，不建就一直回落到上一层：

```
templates/step2_PBE_static/step.conf
templates/step4_HSE_band/step.conf
```

文件夹名 = `skill.yaml` 里的步骤名（`step1a_PBE_opt` / `step1b_PBE_opt` /
`step1c_PBE_opt` / `step2_PBE_static` / `step3_PBE_WAVECAR` / `step4_HSE_band`）。

## 五、日常使用

```bash
# 看这一步最终用什么泛函、从哪来
tf -tt bd -p Mg2C60 -j 1 conf

# 改（写进 project_setting/templates/step1a_PBE_opt/step.conf）
tf -tt bd -p Mg2C60 -j 1 conf --set params.FUNC=pbe-d3

# 想改成"整个材料都用 pbe-d3"，直接编辑共用的那份
vi Fullerene_Network/Mg2C60/band/project_setting/templates/step.conf

# 改完重新生成输入（只生成不提交，先 diff 一下 INCAR）
tf -tt bd -p Mg2C60 -j 1 init
```

`--set` 永远写进**本步的项目文件**，绝不动 skill 出厂默认——「改哪里生效」没有歧义。

## 六、FUNC 的取值与生效链

`project_setting/templates/step.conf`：

```ini
[params]
FUNC = pbesol      # auto | pbe-d3 | pbesol | pbe
```

| 值 | 行为 |
|---|---|
| `pbe-d3` | 强制 PBE+D3(BJ)：INCAR 写 `GGA=PE` + `IVDW=12` |
| `pbesol` | 强制 PBEsol：`GGA=PS`，无色散修正 |
| `pbe` | 强制纯 PBE：`GGA=PE`，无色散修正 |
| `auto` | 嗅探 `incar_*.tpl` 里**写死的** `GGA`/`IVDW` 反推；模板里是 `{{GGA}}` 占位符（正常情况）就取脚本默认值 `pbesol` |

解析出来的值会：

1. 填进 INCAR 的 `{{GGA}}` / `{{VDW_LINE}}` 两个占位符；
2. 写进 `step1x_PBE_opt/workflow_method.txt` 的 `FUNC=` / `GGA=` / `IVDW=`；
3. 由 step2/3/4 的 `detect_method()` 从 `workflow_method.txt` 继承——**这一段本来就是通的，不用改**；
4. `gen_step4_HSE.py` 的 `HYBRID_FLAVOR = "auto"` 据此自动选 HSE06(GGA=PE) 还是 HSEsol(GGA=PS)。

生成后 `validate_generated_incar()` 会复核 INCAR 里的 `GGA`/`IVDW` 与 FUNC 一致，
模板里手写了冲突的 `GGA` 会当场报错。

**所以 FUNC 只需要写在共用的 `templates/step.conf` 里一次**，全流程自洽。

## 七、已验证的行为

| 场景 | 结果 |
|---|---|
| `FUNC = pbe-d3` | `('pbe-d3', 'step.conf')` |
| `FUNC = auto` + 模板是 `{{GGA}}` | `('pbesol', 'auto -> 模板未写死，取脚本默认值')` |
| `FUNC = auto` + 模板写死 `GGA=PE`/`IVDW=12` | `('pbe-d3', 'auto -> 由 incar_3d.tpl 反推')` |
| 材料里没有 `step.conf`（老材料） | `('pbesol', '脚本默认值，老材料兼容')` |
| `step.conf` 的 `STEP=` 与本步不符 | 报错拦截（`gen_need` 串了） |
| `FUNC = scan` | 报错，列出允许值 |
| `[params]` 里有脚本不认识的键 | 报错，列出可用键 |

## 八、`step.conf` 语法（以后加参数时用）

全文只有 `KEY = VALUE`，行尾 `#` 或 `!` 之后是注释。节名即语义：

| 节 | 作用 |
|---|---|
| `[params]` | 脚本行为参数，不写进 INCAR |
| `[submit]` | 覆盖 `submit.sh` 的 Slurm 行；留空 = 沿用模板 |
| `[incar]` | 覆盖从上一步继承来的 INCAR 标签 |
| `[incar.delete]` | 删除继承来的标签，每行一个光秃秃的标签名 |
| `[incar.final]` | 在脚本自动算完 `NBANDS`/`KPAR`/`MAGMOM` 之后再覆盖 |

INCAR 生效顺序固定：`上一步继承 -> [incar] -> 脚本自动计算 -> [incar.final] -> [incar.delete]`

给脚本加一个新参数只要两步：

```python
CONF_SPEC = {
    "FUNC":     (FUNC_DEFAULT, "str"),
    "KSPACING": ("0.03",       "str"),      # <- 新增
}
C = stepconf.load(CONF_SPEC, step_name)
C["KSPACING"]
```

类型支持 `str` / `int` / `float` / `bool` / `words`（空白切分）/
`elemmap`（`Mn:5.0 In:0.0` -> dict）。

## 九、迁移建议

1. 先只 `git apply tf.patch`，跑 `tf` 看状态表正常、老材料行为不变
   （此时还没人读 `step.conf`，纯 no-op）。
2. 装 band 的 4 个文件，`tf -p Mg2C60 init`，确认 `templates/step.conf` 铺出来了。
3. `tf -tt bd -p Mg2C60 -j 1 init` 只生成不提交，`diff` 新旧 `INCAR` 应完全一致。
4. 一致之后再改 `FUNC` 试一次，确认 `GGA` 跟着变、`workflow_method.txt` 也对。
5. 顺手把 `材料/band/` 下平铺的老模板副本删掉——补丁 1 已把「根目录平铺」降到
   最低优先级，留着只会让人误判改了没生效。

## 十、技能需要改吗

**band 需要改两个文件**：

- `skill.yaml` —— `gen_need` 加 `stepconf.py`、`step.conf`（已改好）
- `gen_step1_PBE_opt.py` —— FUNC 改为从 `step.conf` 读（已改好）

**step2 / step3 / step4 的脚本不用动**：它们通过 `workflow_method.txt` 的
`FUNC=` 继承，这条链本来就是通的。

**elastic / ke 暂时不用动**：它们各自的 `gen_step1_*.py` 里 `FUNC = "pbesol"`
还是硬编码。要接的时候照 band 的样子来——拷 `stepconf.py`、加
`templates/step.conf`、脚本里把 `FUNC` 那段换成 `resolve_func()`、
`skill.yaml` 的 `gen_need` 补两个文件。三个技能各写一份 `FUNC` 是对的
（弹性常数用 PBEsol、能带用 PBE-D3 很常见）。
